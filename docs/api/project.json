{
  "name": "api",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "description": "Backend API Documentation for Nicolas Desnoust's Portfolio.",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm task api:build"
      }
    },
    "deploy": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm task api:deploy"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm task api:lint"
      }
    }
  }
}
