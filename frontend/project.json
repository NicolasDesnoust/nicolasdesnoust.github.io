{
  "name": "frontend",
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "task front:build"
      }
    },
    "deploy": {
      "executor": "nx:run-commands",
      "options": {
        "command": "task front:deploy"
      }
    }
  }
}
