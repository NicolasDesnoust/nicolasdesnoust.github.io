{
  "name": "iac",
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm task iac:build"
      }
    },
    "deploy": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm task iac:deploy"
      }
    },
    "format": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm task iac:format"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm task iac:lint"
      }
    },
    "security": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm task iac:security"
      }
    }
  }
}
